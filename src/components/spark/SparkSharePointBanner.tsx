import { SparkSharePointCallout } from '@/components/spark/SparkSharePointCallout';

/** Full-width SharePoint strip on the Spark Offer landing page. */
export function SparkSharePointBanner() {
  return <SparkSharePointCallout variant="banner" />;
}
